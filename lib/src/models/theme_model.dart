import 'package:flutter/material.dart';

class ThemeModel {
  List<Color>? gradient;
  Color? solidColor;
  String? name;

  ThemeModel({
    this.gradient,
    this.solidColor,
    this.name,
  });
}
